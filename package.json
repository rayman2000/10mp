{
  "name": "10mp",
  "version": "1.0.0",
  "description": "10 Minute Pokemon - Collaborative Pokemon Fire Red Experience",
  "scripts": {
    "frontend:dev": "cd frontend && npm start",
    "frontend:build": "cd frontend && npm run build",
    "docker:dev": "cd backend && docker-compose up --build",
    "docker:down": "cd backend && docker-compose down",
    "docker:logs": "cd backend && docker-compose logs -f",
    "docker:db": "cd backend && docker-compose up postgres -d",
    "docker:clean": "cd backend && docker-compose down -v && docker system prune -f",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  },
  "workspaces": [
    "frontend",
    "backend"
  ]
}