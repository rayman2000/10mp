# Docker environment variables for 10MP Backend
# Used by docker compose.yml

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin123

# Frontend Origin (for CORS)
CORS_ORIGIN=http://localhost:3000
